using System;
using UnityEngine;

namespace Exploratorium.Frontend
{
    public class TriggerSync : MonoBehaviour
    {
        public bool Validate(Flow.FlowState state, Action success, Action failure)
        {
            return true;
        }
        
    }
}